{
  "name": "@mcptoolshop/backprop",
  "version": "1.0.0",
  "description": "CLI-first ML trainer with intelligent resource governance",
  "private": false,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mcp-tool-shop-org/backprop.git"
  },
  "author": "mcp-tool-shop <64996768+mcp-tool-shop@users.noreply.github.com>",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "bin": {
    "backprop": "./dist/cli.js"
  },
  "files": [
    "dist",
    "CHANGELOG.md",
    "README.md",
    "logo.png"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "verify": "pnpm build && pnpm test && echo 'All checks passed'",
    "release": "bash scripts/release.sh"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.4.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "ora": "^9.3.0",
    "zod": "^3.22.0"
  }
}
